# Homebrew Tap

This repository hosts Homebrew casks for Ernest Gonzales apps.

## Usage

```bash
brew tap ernest-gonzales/homebrew-tap
brew install --cask codex-tracker
```

## Updating a cask

1. Update the version and SHA256 values in `Casks/codex-tracker.rb`.
2. Commit and push.
